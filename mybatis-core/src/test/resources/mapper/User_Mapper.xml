<mapper namespace="mybatis.diy.dao.IUserDao">

    <select id="queryUserInfoById" parameterType="java.lang.Long" resultType="mybatis.diy.po.User">
        SELECT id, userId, userHead, createTime
        FROM user
        where id = #{id}
    </select>

</mapper>
